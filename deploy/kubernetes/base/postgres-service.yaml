apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: aeai-os
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: autonomous-enterprise-ai-os
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
