apiVersion: v1
kind: Service
metadata:
  name: aeai-api
  namespace: aeai-os
  labels:
    app.kubernetes.io/name: aeai-api
    app.kubernetes.io/part-of: autonomous-enterprise-ai-os
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: aeai-api
  ports:
    - name: http
      port: 8000
      targetPort: http
