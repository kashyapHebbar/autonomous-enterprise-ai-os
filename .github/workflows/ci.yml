name: CI

on:
  pull_request: {}
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  validation:
    name: Lint, test, smoke, demo, and manifests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Verify runner startup
        run: echo "CI runner started"
